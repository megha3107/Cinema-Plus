export const addDataToWatchlist = (data, watchlistId) => {
  //add the data to the selected watchlist
};
export const createWatchlsit = (data, watchlistName) => {
  //create a new watchlist witht the given input and add the selected data to the newly create watchlist
};
export const getUserWatchlists = (userId) => {
  //get all the watchlists names and ids to display them in the watchlist data
};
export const getSingleWatchlistData = (watchlistId) => {
  //get the list of all data(movies/tv-series) from the selected wishlist
};
export const removeDataFromWatchlist = (watchlistId, data) => {
  //remove the selected data from the specified watchlist
};
export const updateWatchlistName = (watchlistId) => {
  //update the name of the specified watchlist with the new given input
};
export const deleteWatchlist = (watchlistId) => {
  //deleted a specified watchlist
};
